// Automatically generated by github.com/fardream/gen-gmsk
// enum for MSKfoldingmode_enum/Foldingmode

package gmsk

// #include <mosek.h>
import "C"

import "strconv"

// Foldingmode is MSKfoldingmode_enum.
//
// Method of folding (symmetry detection for continuous problems).
type Foldingmode uint32

const (
	FOLDING_MODE_OFF               Foldingmode = C.MSK_FOLDING_MODE_OFF               // Disabled.
	FOLDING_MODE_FREE              Foldingmode = C.MSK_FOLDING_MODE_FREE              // The solver decides on the usage and amount of folding.
	FOLDING_MODE_FREE_UNLESS_BASIC Foldingmode = C.MSK_FOLDING_MODE_FREE_UNLESS_BASIC // If only the interior-point solution is requested then the solver decides; if the basic solution is requested then folding is disabled.
	FOLDING_MODE_FORCE             Foldingmode = C.MSK_FOLDING_MODE_FORCE             // Full folding is always performed regardless of workload.
)

var _Foldingmode_map = map[Foldingmode]string{
	FOLDING_MODE_OFF:               "FOLDING_MODE_OFF",
	FOLDING_MODE_FREE:              "FOLDING_MODE_FREE",
	FOLDING_MODE_FREE_UNLESS_BASIC: "FOLDING_MODE_FREE_UNLESS_BASIC",
	FOLDING_MODE_FORCE:             "FOLDING_MODE_FORCE",
}

func (e Foldingmode) String() string {
	if v, ok := _Foldingmode_map[e]; ok {
		return v
	}
	return "Foldingmode(" + strconv.FormatInt(int64(e), 10) + ")"
}
