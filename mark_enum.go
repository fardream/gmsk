// Automatically generated by github.com/fardream/gen-gmsk
// enum for MSKmark_enum/Mark

package gmsk

import "strconv"

// Mark is MSKmark_enum.
//
// Mark
type Mark uint32

const (
	MARK_LO Mark = 0 // The lower bound is selected for sensitivity analysis.
	MARK_UP Mark = 1 // The upper bound is selected for sensitivity analysis.
)

var _Mark_map = map[Mark]string{
	MARK_LO: "MARK_LO",
	MARK_UP: "MARK_UP",
}

func (e Mark) String() string {
	if v, ok := _Mark_map[e]; ok {
		return v
	}
	return "Mark(" + strconv.FormatInt(int64(e), 10) + ")"
}
